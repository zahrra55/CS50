#include <cs50.h>
#include <stdio.h>


int main(void)
{
    long Number;
    do
    {
        Number = get_long("Number: ");
    }
    while(Number < 0);
    if(){//if lohn algorithm is right
        //check the card type
        
    }
    else{
        printf("INVALID\n");
    }
}
bool Luhn_s_Algorithm(long n)
{

}
