#include <cs50.h>
#include <ctype.h>
#include <math.h>
#include <stdio.h>
#include <string.h>

int main(int argc, string argv[])
{
    string key = argv[1];
    int key_len = strlen(key);
    for(int i = 0; i < key_len; i++)
    {
        if (key_len < )
    }
    printf("%s\n",argv[1]);
}
